# OmniRoute - Docker Compose for Coolify
#
# Usage in Coolify:
# 1) Create a Docker Compose application and point it to this repository.
# 2) Use this file as the compose definition.
# 3) Configure required environment variables in Coolify:
#    - JWT_SECRET
#    - API_KEY_SECRET
#    - INITIAL_PASSWORD
#    - NEXT_PUBLIC_BASE_URL (your public https URL)
#    - BASE_URL (same public URL or internal URL as needed)
#
# Notes:
# - Coolify usually handles ingress/proxy automatically, so this file uses expose
#   instead of host port bindings.
# - Data is persisted in /app/data via the named volume below.
# - Includes a CLIProxyAPI sidecar for providers/executors that route through CPA.

services:
  omniroute:
    container_name: omniroute-coolify
    build:
      context: .
      target: runner-cli
    image: omniroute:coolify
    restart: unless-stopped
    stop_grace_period: 40s
    environment:
      NODE_ENV: production
      HOSTNAME: 0.0.0.0

      # Runtime ports
      PORT: ${PORT:-20138}
      DASHBOARD_PORT: ${DASHBOARD_PORT:-${PORT:-20138}}
      API_PORT: ${API_PORT:-20139}
      API_HOST: ${API_HOST:-0.0.0.0}

      # Persistent data location in container
      DATA_DIR: /app/data

      # Public URLs (important for OAuth callbacks and dashboard links)
      BASE_URL: ${BASE_URL:-http://localhost:${PORT:-20138}}
      NEXT_PUBLIC_BASE_URL: ${NEXT_PUBLIC_BASE_URL:-http://localhost:${PORT:-20138}}

      # Security defaults for reverse-proxy deployments
      AUTH_COOKIE_SECURE: ${AUTH_COOKIE_SECURE:-true}
      REQUIRE_API_KEY: ${REQUIRE_API_KEY:-false}

      # Required secrets (set in Coolify environment variables)
      JWT_SECRET: ${JWT_SECRET}
      API_KEY_SECRET: ${API_KEY_SECRET}
      INITIAL_PASSWORD: ${INITIAL_PASSWORD:-CHANGEME}

      # Optional storage encryption
      STORAGE_ENCRYPTION_KEY: ${STORAGE_ENCRYPTION_KEY:-}
      STORAGE_ENCRYPTION_KEY_VERSION: ${STORAGE_ENCRYPTION_KEY_VERSION:-v1}

      # Background services
      PROVIDER_LIMITS_SYNC_INTERVAL_MINUTES: ${PROVIDER_LIMITS_SYNC_INTERVAL_MINUTES:-70}
      OMNIROUTE_DISABLE_BACKGROUND_SERVICES: ${OMNIROUTE_DISABLE_BACKGROUND_SERVICES:-false}

      # CLIProxyAPI sidecar bridge
      CLIPROXYAPI_HOST: ${CLIPROXYAPI_HOST:-cliproxyapi}
      CLIPROXYAPI_PORT: ${CLIPROXYAPI_PORT:-8327}

    expose:
      - "${PORT:-20138}"
      - "${API_PORT:-20139}"

    volumes:
      - omniroute-coolify-data:/app/data

    healthcheck:
      test: ["CMD", "node", "healthcheck.mjs"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  cliproxyapi:
    container_name: cliproxyapi-coolify
    image: kaelsen/cli-proxy-api:latest
    pull_policy: always
    restart: unless-stopped
    environment:
      DEPLOY: production
      HOST: 0.0.0.0
      PORT: ${CLIPROXYAPI_PORT:-8327}
    expose:
      - "${CLIPROXYAPI_PORT:-8327}"
    volumes:
      - ./deploy/cliproxyapi-config.yaml:/CLIProxyAPI/config.yaml:ro
      - cliproxyapi-coolify-logs:/CLIProxyAPI/logs
      - cliproxyapi-coolify-data:/root/.cli-proxy-api
    healthcheck:
      test:
        ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:${CLIPROXYAPI_PORT:-8327}/v1/models"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  omniroute-coolify-data:
    name: omniroute-coolify-data
  cliproxyapi-coolify-logs:
    name: cliproxyapi-coolify-logs
  cliproxyapi-coolify-data:
    name: cliproxyapi-coolify-data
